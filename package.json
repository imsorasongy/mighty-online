{
  "name": "mighty-online",
  "version": "1.0.0",
  "description": "온라인 마이티 카드게임",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd server && npm install",
    "electron": "npx electron electron-main.js",
    "package": "npx @electron/packager . MightyOnline --platform=win32 --arch=x64 --overwrite --ignore=\"(server|dist|\\.git|render\\.yaml|마이티\\.html|온라인마이티\\.bat|\\.gitignore)\""
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
